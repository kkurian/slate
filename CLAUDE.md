# slate

This repository is slate — a task tracker kept in plain markdown, rendered by a
read-only web viewer (`slate.py`). It tracks its own development through the issues
under `issues/`.

When creating or updating issues, follow the tracker conventions in `AGENTS.md`:

@AGENTS.md
