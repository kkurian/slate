# slate

This repository is slate — a task tracker kept in plain markdown, rendered by a
web viewer (`slate.py`) whose only write paths are drag-to-reorder in status views
and the status chip on issue pages. It tracks its own development through the
issues under `issues/`.

When creating or updating issues, follow the tracker conventions in `AGENTS.md`:

@AGENTS.md
